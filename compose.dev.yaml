services:
  cnam-web-dev-database:
    container_name: "cnam-web-dev-database"
    image: "postgres:17.6"
    restart: "unless-stopped"
    env_file: ".env"
    environment:
      POSTGRES_USER: ${DATABASE_USER}
      POSTGRES_PASSWORD: ${DATABASE_PASSWORD}
      POSTGRES_DB: ${DATABASE_NAME}
    ports:
      - "${DATABASE_PORT-5432}:5432"
    volumes:
      - "cnam-web-dev-postgres-data:/var/lib/postgresql/data"

  cnam-web-dev-adminer:
    container_name: "cnam-web-dev-adminer"
    image: "adminneoorg/adminneo:5.1.1"
    restart: "unless-stopped"
    env_file: ".env"
    environment:
      NEO_COLOR_VARIANT: "blue"
      NEO_NAVIGATION_MODE: "dual"
      NEO_PREFER_SELECTION: true
      NEO_DEFAULT_DRIVER: "pgsql"
      NEO_DEFAULT_SERVER: "cnam-web-dev-database"
      NEO_DEFAULT_DATABASE: ${DATABASE_NAME}
    ports:
      - "${ADMINER_PORT-8080}:8080"

volumes:
  cnam-web-dev-postgres-data:
